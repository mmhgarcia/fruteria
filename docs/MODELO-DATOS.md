# Modelo de Datos

> 🚧 **Documentación pendiente.**
> Se completará al finalizar la fase de brainstorming.

---

## Propósito

Describir el esquema completo de almacenamiento: IndexedDB, localStorage, migraciones y backup.

---

## Secciones planeadas

1. Base de datos: `fruteria-db` (IndexedDB)
   - Store `products`: esquema y campos
   - Store `categories`: esquema y campos
   - Store `historico_tasas`: esquema y campos
   - Store `sales`: esquema y campos
   - Store `ramos`: esquema y campos
   - Store `logs`: esquema y campos
2. localStorage: claves y formatos
3. Migraciones (v1 a v6)
4. Backup y restauración
5. Reglas de integridad
