# SPEC-XXX: <Título corto>

> Ficha de trabajo. Una spec = una feature con límites claros.
> Se completa ANTES de implementar. Se marca `Done` al terminar.

> **Convención de rutas:** escribir SIEMPRE desde la raíz del repo (ej. `src/utils/db.js`),
> nunca con `../`. Así las specs se pueden mover de carpeta sin ajustar rutas.

## 1. Problema (¿por qué?)
Describe el dolor concreto que esto resuelve, en una o dos frases. Sin tecnicismos.

## 2. Objetivo (¿qué logramos?)
Resultado esperado, legible por cualquiera. Qué se nota en la app al terminar.

## 3. Alcance (¿qué se hace?) — checklist
- [ ] ...
- [ ] ...

## 4. Fuera de alcance (lo que NO se hace ahora)
Importante para no dispararse y no prometer de más. Listar explícitamente.

## 5. Decisiones / preguntas abiertas (lo que falta definir para aprobar)
Aquí va TODO lo que no está resuelto: opciones a elegir, dudas, lo que falte definir.
Mientras haya algo aquí, la spec NO se implementa. Se van tachando al resolverse.

- [ ] (ej.) ¿Backup local por snapshot o también subida externa?
- [ ] (ej.) ¿Cada cuánto se crea el snapshot?

## 6. Criterios de aceptación (¿cómo sé que quedó bien?)
Cómo lo pruebo manualmente. Pasos concretos que un no-técnico puede seguir.

## 7. Archivos que probablemente tocaré
- Archivos principales (sin necesidad de enumerar cada línea).

## 8. Riesgos / notas
- Cosas que podrían salir mal o decisiones abiertas.

## 9. Estado
- [ ] En definición (se puede crear/tocar; aún no se implementa)
- [ ] Totalmente definida — pendiente de aprobar
- [ ] Aprobada / en implementación
- [ ] Done
